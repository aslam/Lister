<table class="list">
  <caption> <h1> Listing Lists </h1> </caption>
  <col class="col1" />
  <col class="col2" />

  <thead>
    <tr>
      <th>Created By</th>
      <th>Name</th>
      <th>Url</th>
      <th>Description</th>
      <th>Status</th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= link_to list.user.login, edit_admin_user_path(list.user) if list.user %></td>
        <td><%=h list.name %></td>
        <td><%=h list.url %></td>
        <td><%=h truncate(list.description, 60) %></td>
        <td><%=h list.status %></td>
        <td><%= link_to 'Show', admin_list_path(list) %></td>
        <td>
            <%= link_to 'Approve', "/admin/approve/#{list.id}" %> |
            <%= link_to 'Reject', "/admin/reject/#{list.id}" %>
        </td>
        <td><%= link_to 'Destroy', admin_list_path(list), :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br />

<%= link_to 'New list', new_admin_list_path %> |
<%= link_to 'Export to Excel', export_url %>

