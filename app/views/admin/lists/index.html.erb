<h1>Listing lists</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Url</th>
    <th>Description</th>
  </tr>

<% @lists.each do |list| %>
  <tr>
    <td><%=h list.id %></td>
    <td><%=h list.name %></td>
    <td><%=h list.url %></td>
    <td><%=h list.description %></td>
    <td><%= link_to 'Show', list %></td>
    <!-- <td><%= link_to 'Edit', edit_admin_list_path(list) %></td> -->
    <td><%= link_to 'Destroy', list, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New list', new_admin_list_path %> |
<%= link_to 'Export to Excel', export_url %>

